<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.raydairy">
    <!--permission android:name="android.permission.INTERACT_ACROSS_USERS_FULL" android:protectionLevel="signature" /-->

    <application
        android:allowBackup="true"
        android:backupAgent="com.raydairy.MyBackupAgent"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".FirstViewAfterIinstallation">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".SiteDisplay" />
        <activity android:name=".DailyDetails" />
        <activity android:name=".GlobalSettingsActivity" />
        <activity android:name=".RateChart" />
        <activity android:name=".activity_list_view" />
        <activity android:name=".Calculator"/>
        <activity android:name=".MainActivity"></activity>
    </application>

</manifest>